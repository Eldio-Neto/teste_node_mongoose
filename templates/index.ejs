<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redicect</title>
</head>

<body>
    <form action="/" method="POST">
        <input type="text" name="title" placeholder="title" value="<%=body.title%>">
        <input type="text" name="url" placeholder="url"  value="<%=body.url%>">
        <input type="text" name="description" placeholder="description"  value="<%=body.description%>">
        <button>ADD</button>
    </form>
    <%if(error){%>
        <h3>
            <%=error.message%>
        </h3>
    <%}%>
    <a href="/all">Todos os links</a>
</body>
</html>